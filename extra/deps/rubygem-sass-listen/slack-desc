# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

            |-----handy-ruler------------------------------------------------------|
rubygem-sass-listen: rubygem-sass-listen (a CSS extension)
rubygem-sass-listen:
rubygem-sass-listen: Sass makes CSS fun again. Sass is an extension of CSS, adding nested
rubygem-sass-listen: rules, variables, mixins, selector inheritance, and more.
rubygem-sass-listen: It's translated to well-formatted, standard CSS using the command
rubygem-sass-listen: line tool or a web-framework plugin.
rubygem-sass-listen:
rubygem-sass-listen:
rubygem-sass-listen:The Listen gem listens to file modifications and notifies you 
rubygem-sass-listen:about the changes.
rubygem-sass-listen:
rubygem-sass-listen: Homepage: http://sass-lang.com/
rubygem-sass-listen:
rubygem-sass-listen:
rubygem-sass-listen:
